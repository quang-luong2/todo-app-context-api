
const initialState = {
    todos: [],
    valueInput: ''
}

function reducer(state, action) {
    switch(action.type) {
        
        default:
            throw Error('Invalid action...')
    }
}

export { initialState }
export default reducer